% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_interface.R
\name{make_model}
\alias{make_model}
\title{Create a Model Specification for Conformal Prediction}
\usage{
make_model(train_fun, predict_fun, type = c("regression", "classification"))
}
\arguments{
\item{train_fun}{A function with signature \verb{function(x, y)} that takes a
numeric matrix \code{x} and response \code{y} (numeric for regression, factor for
classification) and returns a fitted model object.}

\item{predict_fun}{A function with signature \verb{function(object, x_new)} that
takes a fitted model object and a numeric matrix \code{x_new} and returns
predictions. For regression, must return a numeric vector. For
classification, must return a probability matrix with columns named by
class labels.}

\item{type}{Character string, either \code{"regression"} or \code{"classification"}.}
}
\value{
A \code{predictset_model} object (a list with components \code{train_fun},
\code{predict_fun}, and \code{type}).
}
\description{
Defines how to train a model and generate predictions, allowing any model
to be used with conformal prediction methods.
}
\examples{
reg_model <- make_model(
  train_fun = function(x, y) lm(y ~ ., data = data.frame(y = y, x)),
  predict_fun = function(object, x_new) {
    predict(object, newdata = as.data.frame(x_new))
  },
  type = "regression"
)

}
