% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_cqr.R
\name{conformal_cqr}
\alias{conformal_cqr}
\title{Conformalized Quantile Regression}
\usage{
conformal_cqr(
  x,
  y,
  model_lower,
  model_upper,
  x_new,
  alpha = 0.1,
  cal_fraction = 0.5,
  quantiles = c(0.05, 0.95),
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A numeric vector of response values.}

\item{model_lower}{A \code{\link[=make_model]{make_model()}} specification for the lower quantile
model.}

\item{model_upper}{A \code{\link[=make_model]{make_model()}} specification for the upper quantile
model.}

\item{x_new}{A numeric matrix or data frame of new predictor variables.}

\item{alpha}{Miscoverage level. Default \code{0.10}.}

\item{cal_fraction}{Fraction of data used for calibration. Default \code{0.5}.}

\item{quantiles}{The target quantiles. Default \code{c(0.05, 0.95)}.}

\item{seed}{Optional random seed.}
}
\value{
A \code{predictset_reg} object. See \code{\link[=conformal_split]{conformal_split()}} for details.
The \code{method} component is \code{"cqr"}.
}
\description{
Constructs prediction intervals using Conformalized Quantile Regression
(Romano et al. 2019). Requires two models: one for the lower quantile and
one for the upper quantile. The conformal step adjusts these quantile
predictions to achieve valid coverage.
}
\details{
Interval quality depends on the underlying quantile models. Poorly
calibrated quantile models produce valid but potentially wide intervals.
For best results, use proper quantile regression models (e.g.
\code{quantreg::rq()}) rather than shifted mean predictions.
}
\examples{
set.seed(42)
n <- 200
x <- matrix(rnorm(n * 3), ncol = 3)
y <- x[, 1] * 2 + rnorm(n)
x_new <- matrix(rnorm(20 * 3), ncol = 3)

# Approximating quantile regression with shifted linear models.
# In practice, use quantile regression models, e.g.:
#   quantreg::rq(y ~ ., data = df, tau = 0.05)
model_lo <- make_model(
  train_fun = function(x, y) lm(y ~ ., data = data.frame(y = y, x)),
  predict_fun = function(obj, x_new) {
    predict(obj, newdata = as.data.frame(x_new)) - 1.5
  },
  type = "regression"
)
model_hi <- make_model(
  train_fun = function(x, y) lm(y ~ ., data = data.frame(y = y, x)),
  predict_fun = function(obj, x_new) {
    predict(obj, newdata = as.data.frame(x_new)) + 1.5
  },
  type = "regression"
)

result <- conformal_cqr(x, y, model_lo, model_hi, x_new = x_new)
print(result)

}
\references{
Romano, Y., Patterson, E. and Candes, E.J. (2019).
Conformalized quantile regression.
\emph{Advances in Neural Information Processing Systems}, 32.
\doi{10.48550/arXiv.1905.03222}
}
\seealso{
Other regression methods: 
\code{\link{conformal_aci}()},
\code{\link{conformal_cv}()},
\code{\link{conformal_jackknife}()},
\code{\link{conformal_mondrian}()},
\code{\link{conformal_split}()},
\code{\link{conformal_weighted}()}
}
\concept{regression methods}
