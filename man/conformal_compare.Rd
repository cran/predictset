% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{conformal_compare}
\alias{conformal_compare}
\title{Compare Conformal Prediction Methods}
\usage{
conformal_compare(
  x,
  y,
  model,
  x_new,
  y_new,
  methods = c("split", "cv"),
  alpha = 0.1,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A numeric vector of response values.}

\item{model}{A fitted model object, a \code{\link[=make_model]{make_model()}} specification, or a
formula.}

\item{x_new}{A numeric matrix or data frame of new predictor variables.}

\item{y_new}{A numeric vector of true response values for \code{x_new}, used to
compute empirical coverage.}

\item{methods}{Character vector of method names to compare. Default
\code{c("split", "cv")}. Available methods: \code{"split"}, \code{"cv"}, \code{"jackknife"},
\code{"jackknife_basic"}.}

\item{alpha}{Miscoverage level. Default \code{0.10}.}

\item{seed}{Optional random seed.}
}
\value{
A \code{predictset_compare} object (a data frame) with columns:
\describe{
\item{method}{Character. The method name.}
\item{coverage}{Numeric. Empirical coverage on \code{y_new}.}
\item{mean_width}{Numeric. Mean interval width.}
\item{median_width}{Numeric. Median interval width.}
\item{time_seconds}{Numeric. Elapsed time in seconds.}
}
}
\description{
Runs multiple conformal prediction methods on the same data and returns a
comparison data frame with coverage, interval width, and computation time
for each method.
}
\examples{
set.seed(42)
n <- 300
x <- matrix(rnorm(n * 3), ncol = 3)
y <- x[, 1] * 2 + rnorm(n)
x_new <- matrix(rnorm(100 * 3), ncol = 3)
y_new <- x_new[, 1] * 2 + rnorm(100)

\donttest{
comp <- conformal_compare(x, y, model = y ~ ., x_new = x_new,
                           y_new = y_new)
print(comp)
}

}
\seealso{
Other diagnostics: 
\code{\link{conformal_pvalue}()},
\code{\link{coverage}()},
\code{\link{coverage_by_bin}()},
\code{\link{coverage_by_group}()},
\code{\link{interval_width}()},
\code{\link{set_size}()}
}
\concept{diagnostics}
