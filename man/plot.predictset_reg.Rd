% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.predictset_reg}
\alias{plot.predictset_reg}
\title{Plot Method for Regression Conformal Objects}
\usage{
\method{plot}{predictset_reg}(x, max_points = 200, ...)
}
\arguments{
\item{x}{A \code{predictset_reg} object.}

\item{max_points}{Maximum number of points to display. Default \code{200}.
If there are more predictions, a random subset is shown.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}}.}
}
\value{
The input object, invisibly.
}
\description{
Creates a base R plot showing prediction intervals. Points are ordered
by predicted value, with intervals shown as vertical segments.
}
\examples{
set.seed(42)
x <- matrix(rnorm(200 * 3), ncol = 3)
y <- x[, 1] * 2 + rnorm(200)
x_new <- matrix(rnorm(50 * 3), ncol = 3)

result <- conformal_split(x, y, model = y ~ ., x_new = x_new)
plot(result)

}
