% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_cv.R
\name{conformal_cv}
\alias{conformal_cv}
\title{CV+ Conformal Prediction Intervals}
\usage{
conformal_cv(
  x,
  y,
  model,
  x_new = NULL,
  alpha = 0.1,
  n_folds = 10,
  verbose = FALSE,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A numeric vector of response values.}

\item{model}{A fitted model object (e.g., from \code{\link[=lm]{lm()}}), a \code{\link[=make_model]{make_model()}}
specification, or a formula (which will fit a linear model).}

\item{x_new}{A numeric matrix or data frame of new predictor variables.
If \code{NULL}, intervals are computed for the training data using
leave-one-fold-out predictions. Note: when \code{x_new = NULL}, prediction
intervals for training observations use a self-consistent approximation.
For exact CV+ intervals on new data, provide \code{x_new}.}

\item{alpha}{Miscoverage level. Default \code{0.10} gives 90 percent prediction
intervals.}

\item{n_folds}{Number of cross-validation folds. Default \code{10}.}

\item{verbose}{Logical. If \code{TRUE}, shows a progress bar during fold fitting.
Default \code{FALSE}.}

\item{seed}{Optional random seed for reproducible data splitting.}
}
\value{
A \code{predictset_reg} object. See \code{\link[=conformal_split]{conformal_split()}} for details.
The \code{method} component is \code{"cv_plus"}. The object also stores
\code{fold_models} (list of K fitted models), \code{fold_ids} (integer vector
mapping each observation to its fold), and \code{residuals} (leave-fold-out
absolute residuals), which are needed by the \code{\link[=predict]{predict()}} method to
compute CV+ intervals for new data.
}
\description{
Constructs prediction intervals using the CV+ method of Barber et al. (2021).
Cross-validation residuals and fold-specific models are used to form
observation-specific prediction intervals with finite-sample coverage
guarantees.
}
\details{
Unlike basic CV conformal prediction (which computes a single quantile of
CV residuals), CV+ constructs intervals that vary per test point. For each
test point, every training observation contributes a lower and upper value
based on the fold model that excluded that observation, evaluated at the
test point, plus or minus the leave-fold-out residual for that observation.
The interval bounds are then taken as quantiles of these per-observation
values.

The CV+ theoretical coverage guarantee is \eqn{1 - 2\alpha}, not
\eqn{1 - \alpha} (Barber et al. 2021, Theorem 2). This is weaker than
split conformal's \eqn{1 - \alpha} guarantee. In practice, CV+ coverage
is typically much closer to \eqn{1 - \alpha}.
}
\examples{
set.seed(42)
n <- 200
x <- matrix(rnorm(n * 3), ncol = 3)
y <- x[, 1] * 2 + rnorm(n)
x_new <- matrix(rnorm(20 * 3), ncol = 3)

\donttest{
result <- conformal_cv(x, y, model = y ~ ., x_new = x_new, n_folds = 5)
print(result)
}

}
\references{
Barber, R.F., Candes, E.J., Ramdas, A. and Tibshirani, R.J. (2021).
Predictive inference with the Jackknife+.
\emph{Annals of Statistics}, 49(1), 486-507.
\doi{10.1214/20-AOS1965}
}
\seealso{
Other regression methods: 
\code{\link{conformal_aci}()},
\code{\link{conformal_cqr}()},
\code{\link{conformal_jackknife}()},
\code{\link{conformal_mondrian}()},
\code{\link{conformal_split}()},
\code{\link{conformal_weighted}()}
}
\concept{regression methods}
