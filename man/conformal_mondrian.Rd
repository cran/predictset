% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_mondrian.R
\name{conformal_mondrian}
\alias{conformal_mondrian}
\title{Mondrian Conformal Prediction Intervals (Group-Conditional)}
\usage{
conformal_mondrian(
  x,
  y,
  model,
  x_new,
  groups,
  groups_new,
  alpha = 0.1,
  cal_fraction = 0.5,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A numeric vector of response values.}

\item{model}{A fitted model object, a \code{\link[=make_model]{make_model()}} specification, or a
formula.}

\item{x_new}{A numeric matrix or data frame of new predictor variables.}

\item{groups}{A factor or character vector of group labels for each
observation in \code{x}, with length equal to \code{nrow(x)}.}

\item{groups_new}{A factor or character vector of group labels for each
observation in \code{x_new}, with length equal to \code{nrow(x_new)}.}

\item{alpha}{Miscoverage level. Default \code{0.10}.}

\item{cal_fraction}{Fraction of data used for calibration. Default \code{0.5}.}

\item{seed}{Optional random seed.}
}
\value{
A \code{predictset_reg} object. See \code{\link[=conformal_split]{conformal_split()}} for details.
The \code{method} component is \code{"mondrian"}. Additional components include
\code{groups_new} (the group labels for new data) and \code{group_quantiles}
(named numeric vector of per-group conformal quantiles).
}
\description{
Constructs prediction intervals with group-conditional coverage guarantees.
Instead of a single conformal quantile, a separate quantile is computed for
each group, ensuring coverage within each subgroup (e.g. by gender, region,
or model type).
}
\examples{
set.seed(42)
n <- 400
x <- matrix(rnorm(n * 3), ncol = 3)
groups <- factor(ifelse(x[, 1] > 0, "high", "low"))
y <- x[, 1] * 2 + ifelse(groups == "high", 2, 0.5) * rnorm(n)
x_new <- matrix(rnorm(50 * 3), ncol = 3)
groups_new <- factor(ifelse(x_new[, 1] > 0, "high", "low"))

\donttest{
result <- conformal_mondrian(x, y, model = y ~ ., x_new = x_new,
                              groups = groups, groups_new = groups_new)
print(result)
}

}
\references{
Vovk, V., Gammerman, A. and Shafer, G. (2005).
\emph{Algorithmic Learning in a Random World}. Springer.
}
\seealso{
Other regression methods: 
\code{\link{conformal_aci}()},
\code{\link{conformal_cqr}()},
\code{\link{conformal_cv}()},
\code{\link{conformal_jackknife}()},
\code{\link{conformal_split}()},
\code{\link{conformal_weighted}()}
}
\concept{regression methods}
