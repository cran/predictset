% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.predictset_reg}
\alias{predict.predictset_reg}
\title{Predict Method for Regression Conformal Objects}
\usage{
\method{predict}{predictset_reg}(object, newdata, ...)
}
\arguments{
\item{object}{A \code{predictset_reg} object.}

\item{newdata}{A numeric matrix or data frame of new predictor variables.}

\item{...}{Additional arguments. For Mondrian objects, pass
\code{groups_new} (a factor or character vector of group labels for each
observation in \code{newdata}).}
}
\value{
A data frame with columns \code{pred}, \code{lower}, and \code{upper}.
}
\description{
Generate prediction intervals for new data using a fitted conformal
prediction object.
}
\examples{
set.seed(42)
x <- matrix(rnorm(200 * 3), ncol = 3)
y <- x[, 1] * 2 + rnorm(200)
x_new <- matrix(rnorm(10 * 3), ncol = 3)

result <- conformal_split(x, y, model = y ~ ., x_new = x_new)
preds <- predict(result, newdata = matrix(rnorm(5 * 3), ncol = 3))

}
