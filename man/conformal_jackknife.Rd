% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_jackknife.R
\name{conformal_jackknife}
\alias{conformal_jackknife}
\title{Jackknife+ Conformal Prediction Intervals}
\usage{
conformal_jackknife(
  x,
  y,
  model,
  x_new = NULL,
  alpha = 0.1,
  plus = TRUE,
  verbose = FALSE,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A numeric vector of response values.}

\item{model}{A fitted model object (e.g., from \code{\link[=lm]{lm()}}), a \code{\link[=make_model]{make_model()}}
specification, or a formula (which will fit a linear model).}

\item{x_new}{A numeric matrix or data frame of new predictor variables.
If \code{NULL}, intervals are computed for the training data.}

\item{alpha}{Miscoverage level. Default \code{0.10} gives 90 percent prediction
intervals.}

\item{plus}{Logical. If \code{TRUE} (default), uses the Jackknife+ method.
If \code{FALSE}, uses the basic jackknife.}

\item{verbose}{Logical. If \code{TRUE}, shows a progress bar during LOO fitting.
Default \code{FALSE}.}

\item{seed}{Optional random seed for reproducible data splitting.}
}
\value{
A \code{predictset_reg} object. See \code{\link[=conformal_split]{conformal_split()}} for details.
The \code{method} component is \code{"jackknife_plus"} or \code{"jackknife"}.
Additional components include \code{loo_models} (list of n leave-one-out
fitted models) and \code{loo_residuals} (numeric vector of LOO absolute
residuals).
}
\description{
Constructs prediction intervals using the Jackknife+ method of
Barber et al. (2021). Uses leave-one-out models to form prediction
intervals with finite-sample coverage guarantees.
}
\details{
The Jackknife+ method fits n leave-one-out models and uses each model's
prediction at the test point, shifted by the corresponding LOO residual,
to construct the interval. This is distinct from basic jackknife, which
centres a single full-model prediction and adds a quantile of the LOO
residuals.

The Jackknife+ theoretical coverage guarantee is \eqn{1 - 2\alpha}, not
\eqn{1 - \alpha} (Barber et al. 2021, Theorem 1). This is weaker than
split conformal's \eqn{1 - \alpha} guarantee. In practice, Jackknife+
coverage is typically much closer to \eqn{1 - \alpha}.
}
\examples{
set.seed(42)
n <- 50
x <- matrix(rnorm(n * 2), ncol = 2)
y <- x[, 1] + rnorm(n)
x_new <- matrix(rnorm(10 * 2), ncol = 2)

\donttest{
result <- conformal_jackknife(x, y, model = y ~ ., x_new = x_new)
print(result)
}

}
\references{
Barber, R.F., Candes, E.J., Ramdas, A. and Tibshirani, R.J. (2021).
Predictive inference with the jackknife+.
\emph{Annals of Statistics}, 49(1), 486--507.
}
\seealso{
Other regression methods: 
\code{\link{conformal_aci}()},
\code{\link{conformal_cqr}()},
\code{\link{conformal_cv}()},
\code{\link{conformal_mondrian}()},
\code{\link{conformal_split}()},
\code{\link{conformal_weighted}()}
}
\concept{regression methods}
