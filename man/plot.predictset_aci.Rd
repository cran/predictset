% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.predictset_aci}
\alias{plot.predictset_aci}
\title{Plot Method for ACI Objects}
\usage{
\method{plot}{predictset_aci}(x, max_points = 500, ...)
}
\arguments{
\item{x}{A \code{predictset_aci} object.}

\item{max_points}{Maximum number of points to display. Default \code{500}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
The input object, invisibly.
}
\description{
Creates a two-panel base R plot. The top panel shows prediction intervals
over time; the bottom panel shows the adaptive alpha trace.
}
\examples{
set.seed(42)
n <- 100
y_true <- cumsum(rnorm(n, sd = 0.1)) + rnorm(n)
y_pred <- c(0, y_true[-n])

\donttest{
result <- conformal_aci(y_pred, y_true, alpha = 0.10, gamma = 0.01)
plot(result)
}

}
