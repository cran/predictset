% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{coverage_by_group}
\alias{coverage_by_group}
\title{Empirical Coverage by Group}
\usage{
coverage_by_group(object, y_true, groups)
}
\arguments{
\item{object}{A \code{predictset_reg} or \code{predictset_class} object.}

\item{y_true}{True values (numeric for regression, factor/character for
classification).}

\item{groups}{A factor or character vector of group labels with the same
length as the number of predictions.}
}
\value{
A data frame with columns \code{group}, \code{coverage}, \code{n}, and \code{target}.
}
\description{
Computes empirical coverage within each group, useful for diagnosing
conditional coverage violations.
}
\examples{
set.seed(42)
n <- 500
x <- matrix(rnorm(n * 3), ncol = 3)
y <- x[, 1] * 2 + rnorm(n)
x_new <- matrix(rnorm(200 * 3), ncol = 3)
y_new <- x_new[, 1] * 2 + rnorm(200)
groups <- factor(ifelse(x_new[, 1] > 0, "high", "low"))

result <- conformal_split(x, y, model = y ~ ., x_new = x_new)
coverage_by_group(result, y_new, groups)

}
\seealso{
Other diagnostics: 
\code{\link{conformal_compare}()},
\code{\link{conformal_pvalue}()},
\code{\link{coverage}()},
\code{\link{coverage_by_bin}()},
\code{\link{interval_width}()},
\code{\link{set_size}()}
}
\concept{diagnostics}
