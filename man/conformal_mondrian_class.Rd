% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_mondrian.R
\name{conformal_mondrian_class}
\alias{conformal_mondrian_class}
\title{Mondrian Conformal Prediction Sets for Classification}
\usage{
conformal_mondrian_class(
  x,
  y,
  model,
  x_new,
  groups,
  groups_new,
  alpha = 0.1,
  cal_fraction = 0.5,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A factor (or character/integer vector coerced to factor) of class
labels.}

\item{model}{A \code{\link[=make_model]{make_model()}} specification with \code{type = "classification"},
or a fitted model object.}

\item{x_new}{A numeric matrix or data frame of new predictor variables.}

\item{groups}{A factor or character vector of group labels for each
observation in \code{x}.}

\item{groups_new}{A factor or character vector of group labels for each
observation in \code{x_new}.}

\item{alpha}{Miscoverage level. Default \code{0.10}.}

\item{cal_fraction}{Fraction of data used for calibration. Default \code{0.5}.}

\item{seed}{Optional random seed.}
}
\value{
A \code{predictset_class} object. See \code{\link[=conformal_lac]{conformal_lac()}} for
details. The \code{method} component is \code{"mondrian"}. Additional components
include \code{groups_new} and \code{group_quantiles}.
}
\description{
Constructs prediction sets with group-conditional coverage guarantees for
classification. Uses LAC-style scoring with per-group conformal quantiles.
}
\examples{
set.seed(42)
n <- 400
x <- matrix(rnorm(n * 4), ncol = 4)
groups <- factor(ifelse(x[, 1] > 0, "high", "low"))
y <- factor(ifelse(x[,1] + x[,2] > 0, "A", "B"))
x_new <- matrix(rnorm(50 * 4), ncol = 4)
groups_new <- factor(ifelse(x_new[, 1] > 0, "high", "low"))

clf <- make_model(
  train_fun = function(x, y) glm(y ~ ., data = data.frame(y = y, x),
                                  family = "binomial"),
  predict_fun = function(object, x_new) {
    df <- as.data.frame(x_new)
    names(df) <- paste0("X", seq_len(ncol(x_new)))
    p <- predict(object, newdata = df, type = "response")
    cbind(A = 1 - p, B = p)
  },
  type = "classification"
)

\donttest{
result <- conformal_mondrian_class(x, y, model = clf, x_new = x_new,
                                    groups = groups, groups_new = groups_new)
print(result)
}

}
\seealso{
Other classification methods: 
\code{\link{conformal_aps}()},
\code{\link{conformal_lac}()},
\code{\link{conformal_raps}()}
}
\concept{classification methods}
