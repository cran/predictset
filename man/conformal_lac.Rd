% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_lac.R
\name{conformal_lac}
\alias{conformal_lac}
\title{Least Ambiguous Classifier Prediction Sets}
\usage{
conformal_lac(x, y, model, x_new, alpha = 0.1, cal_fraction = 0.5, seed = NULL)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A factor (or character/integer vector coerced to factor) of class
labels.}

\item{model}{A \code{\link[=make_model]{make_model()}} specification with \code{type = "classification"},
or a fitted model object that produces class probabilities.}

\item{x_new}{A numeric matrix or data frame of new predictor variables.}

\item{alpha}{Miscoverage level. Default \code{0.10} gives 90 percent prediction sets.}

\item{cal_fraction}{Fraction of data used for calibration. Default \code{0.5}.}

\item{seed}{Optional random seed.}
}
\value{
A \code{predictset_class} object with components:
\describe{
\item{sets}{A list of character vectors, one per new observation.}
\item{probs}{A list of named numeric vectors with predicted probabilities
for included classes.}
\item{alpha}{The miscoverage level used.}
\item{method}{Character string \code{"lac"}.}
\item{scores}{Numeric vector of calibration scores.}
\item{quantile}{The conformal quantile used.}
\item{classes}{Character vector of all class labels.}
\item{n_cal}{Number of calibration observations.}
\item{n_train}{Number of training observations.}
\item{fitted_model}{The fitted model object.}
\item{model}{The \code{predictset_model} specification.}
}
}
\description{
Constructs prediction sets using the Least Ambiguous Classifier (LAC)
method. Includes all classes whose predicted probability exceeds
\code{1 - q}, where \code{q} is the conformal quantile of \verb{1 - p(true class)} scores.
}
\examples{
set.seed(42)
n <- 300
x <- matrix(rnorm(n * 4), ncol = 4)
y <- factor(ifelse(x[,1] + x[,2] > 0, "A", "B"))
x_new <- matrix(rnorm(50 * 4), ncol = 4)

clf <- make_model(
  train_fun = function(x, y) glm(y ~ ., data = data.frame(y = y, x),
                                  family = "binomial"),
  predict_fun = function(object, x_new) {
    df <- as.data.frame(x_new)
    names(df) <- paste0("X", seq_len(ncol(x_new)))
    p <- predict(object, newdata = df, type = "response")
    cbind(A = 1 - p, B = p)
  },
  type = "classification"
)

result <- conformal_lac(x, y, model = clf, x_new = x_new)
print(result)

}
\references{
Sadinle, M., Lei, J. and Wasserman, L. (2019).
Least ambiguous set-valued classifiers with bounded error levels.
\emph{Journal of the American Statistical Association}, 114(525), 223-234.
\doi{10.1080/01621459.2017.1395341}
}
\seealso{
Other classification methods: 
\code{\link{conformal_aps}()},
\code{\link{conformal_mondrian_class}()},
\code{\link{conformal_raps}()}
}
\concept{classification methods}
