% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_split.R
\name{conformal_split}
\alias{conformal_split}
\title{Split Conformal Prediction Intervals}
\usage{
conformal_split(
  x,
  y,
  model,
  x_new,
  alpha = 0.1,
  cal_fraction = 0.5,
  score_type = c("absolute", "normalized"),
  scale_model = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictor variables.}

\item{y}{A numeric vector of response values.}

\item{model}{A fitted model object (e.g., from \code{\link[=lm]{lm()}}), a \code{\link[=make_model]{make_model()}}
specification, or a formula (which will fit a linear model).}

\item{x_new}{A numeric matrix or data frame of new predictor variables for
which to compute prediction intervals.}

\item{alpha}{Miscoverage level. Default \code{0.10} gives 90 percent prediction
intervals.}

\item{cal_fraction}{Fraction of data used for calibration. Default \code{0.5}.}

\item{score_type}{Type of nonconformity score. \code{"absolute"} (default) uses
absolute residuals and produces constant-width intervals. \code{"normalized"}
divides residuals by a local scale estimate from \code{scale_model}, producing
locally-adaptive interval widths.}

\item{scale_model}{A \code{\link[=make_model]{make_model()}} specification for predicting absolute
residuals (used only when \code{score_type = "normalized"}). Must return
positive predictions. If \code{NULL} and \code{score_type = "normalized"}, a
default model is fitted using \code{\link[=lm]{lm()}} on absolute residuals.}

\item{seed}{Optional random seed for reproducible data splitting.}
}
\value{
A \code{predictset_reg} object (a list) with components:
\describe{
\item{pred}{Numeric vector of point predictions for \code{x_new}.}
\item{lower}{Numeric vector of lower bounds.}
\item{upper}{Numeric vector of upper bounds.}
\item{alpha}{The miscoverage level used.}
\item{method}{Character string \code{"split"}.}
\item{scores}{Numeric vector of calibration nonconformity scores.}
\item{quantile}{The conformal quantile used to form intervals.}
\item{n_cal}{Number of calibration observations.}
\item{n_train}{Number of training observations.}
\item{fitted_model}{The fitted model object.}
\item{model}{The \code{predictset_model} specification.}
}
}
\description{
Constructs prediction intervals using split conformal inference. The data
is split into training and calibration sets; nonconformity scores are
computed on the calibration set and used to form intervals on new data.
}
\examples{
set.seed(42)
n <- 200
x <- matrix(rnorm(n * 3), ncol = 3)
y <- x[, 1] * 2 + rnorm(n)
x_new <- matrix(rnorm(50 * 3), ncol = 3)

result <- conformal_split(x, y, model = y ~ ., x_new = x_new)
print(result)

}
\references{
Lei, J., G'Sell, M., Rinaldo, A., Tibshirani, R.J. and Wasserman, L. (2018).
Distribution-free predictive inference for regression.
\emph{Journal of the American Statistical Association}, 113(523), 1094-1111.
\doi{10.1080/01621459.2017.1307116}
}
\seealso{
Other regression methods: 
\code{\link{conformal_aci}()},
\code{\link{conformal_cqr}()},
\code{\link{conformal_cv}()},
\code{\link{conformal_jackknife}()},
\code{\link{conformal_mondrian}()},
\code{\link{conformal_weighted}()}
}
\concept{regression methods}
